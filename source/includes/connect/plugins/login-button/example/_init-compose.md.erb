> When you need multiple plugins usage, just combine them by EndpassComposePlugin

```javascript
// If you are using modules or npm
import EndpassComposePlugin from '@endpass/connect/compose';
import EndpassLoginButtonPlugin from '@endpass/connect/loginButton';

// Insert your client ID here
const YOUR_CLIENT_ID = '';

// Initialize Connect with clientId from vault
const connect = new EndpassComposePlugin({
  clientId: YOUR_CLIENT_ID,
  plugins: [
    EndpassLoginButtonPlugin
  ]
});
