## Usage

Create a new instance of the connect client with your application's Client ID and custom configuration options.

<%= partial "includes/connect/plugins/oauth/example/_example.md.erb" %>

The connect client wraps a standard oAuth2 REST client instance. You can pass parameters to the `request` method to access any [server API endpoints](#server-api).

