# Endpass Connect client

## Installation (npm)

The Javascript client library can be installed with a package manager like npm, or directly included in an HTML file from a script tag.

Install the library via `npm` or `yarn`.

<%= partial "includes/connect/example/_install.md.erb" %>

<%= partial "includes/connect/_cdn.md.erb" %>

### Content Security Policy

If you want to use Endpass Connect with CSP rules, just copy/paste this code into the `<head>` tag of your html page.

> Paste this code into your head tag if you are getting CSP errors

```html
<meta http-equiv="Content-Security-Policy" content="default-src 'self' data: *.endpass.com; script-src 'self' data: 'unsafe-inline' 'unsafe-eval' *.endpass.com unpkg.com; object-src 'self' *.endpass.com; style-src 'self' data: 'unsafe-inline' *.endpass.com; img-src 'self' *.endpass.com; media-src 'self' *.endpass.com; frame-src 'self' *.endpass.com; font-src 'self' *.endpass.com; connect-src 'self' *.endpass.com"/>
```


### Init instance

<%= partial "includes/connect/example/_init-instance.md.erb" %>

Create instance of class and use it in your application. You can know about
options and methods in the [Instance methods section](#instance-methods) and [Plugin section](#plugins). For handle errors see the [Error section](#connect-errors).

## Plugins

Endpass Connect supports mountable plugins. By default the library includes the [OAuth plugin](#oauth-plugin).

|Name|Description|
|-|-|
|[OAuth](#oauth-plugin)|For getting private user information from [Server API](#server-api)|


<%= partial "includes/connect/plugins/oauth/_oauth.md.erb" %>

<%= partial "includes/connect/plugins/login-button/_login-button.md.erb" %>

<%= partial "includes/connect/_methods.md.erb" %>

<%= partial "includes/connect/_errors.md.erb" %>
